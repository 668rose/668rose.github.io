<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>指针做函数参数的问题</title>
      <link href="/2022/02/09/zhi-zhen-zuo-han-shu-can-shu-de-wen-ti/"/>
      <url>/2022/02/09/zhi-zhen-zuo-han-shu-can-shu-de-wen-ti/</url>
      
        <content type="html"><![CDATA[<h1 id="C语言中指针做函数参数的易错问题"><a href="#C语言中指针做函数参数的易错问题" class="headerlink" title="C语言中指针做函数参数的易错问题"></a>C语言中指针做函数参数的易错问题</h1><p> 在第一次编写创建链表的函数时，我是这样写的;<br> 是在主函数定义了:<br> struct student* p;<br> p&#x3D;NULL;<br> 将p作为input函数形参;而函数没有返回值；<br> 如下:<br>void input(struct student* head)<br>{</p><h2 id="struct-student-p2-p1"><a href="#struct-student-p2-p1" class="headerlink" title="struct student* p2, * p1;"></a>struct student* p2, * p1;</h2><pre><code>p1 = p2 = (struct student*)malloc(LEN);scanf_s(&quot;%d&quot;, &amp;p1-&gt;number);scanf_s(&quot;%s&quot;, &amp;p1-&gt;name, 10);scanf_s(&quot;%f&quot;, &amp;p1-&gt;mark);head = p1; //试图改变head指针，来改变pfor (;p1-&gt;number != 0;)&#123;    p1 = (struct student*)malloc(LEN);    scanf_s(&quot;%d&quot;, &amp;p1-&gt;number);    scanf_s(&quot;%s&quot;, &amp;p1-&gt;name, 10);    scanf_s(&quot;%f&quot;, &amp;p1-&gt;mark);    p2-&gt;next = p1;    p2 = p1;&#125;</code></pre><p>}<br> 试图这样改变主函数中的p;<br> 而实际执行后主函数中的p并没有指向head;<br> p依然是NULL;</p><p> 在翻书后发现书中使用函数返回值来实现改变p的指向;<br> 于是，我作出如下改变,结果可行;<br> struct student* input(void)<br>{</p><h2 id="struct-student-p1-p2-head"><a href="#struct-student-p1-p2-head" class="headerlink" title="struct student* p1, * p2, * head;"></a>struct student* p1, * p2, * head;</h2><pre><code>p1 = p2 = (struct student*)malloc(LEN);scanf_s(&quot;%d&quot;, &amp;p1-&gt;number);scanf_s(&quot;%s&quot;, &amp;p1-&gt;name, 10);scanf_s(&quot;%f&quot;, &amp;p1-&gt;mark);head = p1;for (;p1-&gt;number != 0;)&#123;    p1 = (struct student*)malloc(LEN);    scanf_s(&quot;%d&quot;, &amp;p1-&gt;number);    scanf_s(&quot;%s&quot;, &amp;p1-&gt;name, 10);    scanf_s(&quot;%f&quot;, &amp;p1-&gt;mark);    p2-&gt;next = p1;    p2 = p1;&#125;return head;</code></pre><p>}</p><p>可为什么最初的方法不行呢？指针不是传地址吗？<br>input函数中改变head的地址了呀;这令我百思不得其解;<br>直到我看到BraveY的一篇博客文章，<br>其中讲到:<br>指针形参也是一种‘值的传递’，只是因为传递的是一个对象的地址，<br>因此可以通过解引用符<em>来对该对象直接进行修改，<br>所以在被调用函数中可以直接根据地址来进行操作，<br>从而使得该地址的变量被修改。<br>正确写法:<br>首先明确这段代码的意图是想让p指针这个变量的值在被调用函数中发生变化，<br>因为需要修改实参变量的值，所以需要传入实参的地址，<br>指向指针的地址也就是一个二级指针。<br>所以形参应该是一个二级指针，<br>在被调用函数中使用</em>来对地址进行解引用。<br>看到这里我恍然大悟!<br>将代码改为:<br>主函数中:<br>input(&amp;p);<br>input函数中:<br>void input(struct student** p)<br>{<br>    struct student* p1, * p2, * head;<br>    p1 &#x3D; p2 &#x3D; (struct student*)malloc(LEN);<br>    scanf_s(“%d”, &amp;p1-&gt;number);<br>    scanf_s(“%s”, &amp;p1-&gt;name, 10);<br>    scanf_s(“%f”, &amp;p1-&gt;mark);<br>    head &#x3D; p1;<br>    for (;p1-&gt;number !&#x3D; 0;)<br>    {<br>        p1 &#x3D; (struct student*)malloc(LEN);<br>        scanf_s(“%d”, &amp;p1-&gt;number);<br>        scanf_s(“%s”, &amp;p1-&gt;name, 10);<br>        scanf_s(“%f”, &amp;p1-&gt;mark);<br>        p2-&gt;next &#x3D; p1;<br>        p2 &#x3D; p1;<br>    }<br>    *p &#x3D; head;<br>}</p><p>经测试，没有问题。<br>你可能会说，这么麻烦干嘛……（擦汗）<br>因为这样可以实现函数多个返回值（机智），哈哈^_^</p>]]></content>
      
      
      <categories>
          
          <category> C语言 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C语言 </tag>
            
            <tag> C函数 </tag>
            
            <tag> C指针 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
